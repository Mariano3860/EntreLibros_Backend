# ---------- Local-only overrides ----------
# IMPORTANT: Do NOT commit real secrets. Replace with your own secure value (>=32 bytes).
# Example (PowerShell) to generate a 32-byte Base64:
#   $b = New-Object byte[] 32; [Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($b); [Convert]::ToBase64String($b)

jwt.secret=REPLACE_WITH_A_SECURE_BASE64_32B_OR_MORE
spring.datasource.password=postgres

# If you prefer, you can also override these locally (optional):
# server.port=4000
# spring.datasource.url=jdbc:postgresql://localhost:5432/entrelibros
# spring.datasource.username=postgres
# spring.datasource.password=postgres
